# ProtonForge Python RPC
# Módulos padrão apenas, mas estas garantem compatibilidade
# Instalado via pyenv Python 3.10 venv
aiohttp>=3.8,<4
yarl>=1.9,<2
multidict>=6,<7
frozenlist>=1,<2
aiosignal>=1,<2
attrs>=23,<25
